
extends Light2D

var velocity = Vector2.ZERO
var random = RandomNumberGenerator.new()

func _process(delta):
	pass
#	random.randomize()
#	velocity.y += rand_range(0.5, -0.5) * delta
#	velocity.x += rand_range(-0.5, 0.5) * delta
#	offset += velocity
#	velocity -= offset / 10
